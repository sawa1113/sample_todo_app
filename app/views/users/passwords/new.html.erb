<div class="container mt-5" style="max-width: 500px;">
  <h2 class="text-center mb-4">パスワード再発行画面</h2>

  <p class="text-muted text-center mb-4">
    以下のメールアドレスを入力すると、パスワードをリセットするための手順が送信されます。
  </p>

  <div class="card shadow-sm p-4">
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "needs-validation", novalidate: true }) do |f| %>
      <%= f.error_notification %>

      <!-- メールアドレス入力 -->
      <div class="form-group mb-3">
        <label for="email" class="form-label">メールアドレス</label>
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    input_html: { class: "form-control", autocomplete: "email", id: "email" },
                    label: false %>
      </div>

      <!-- 送信ボタン -->
      <div class="d-grid">
        <%= f.button :submit, "送信する", class: "btn btn-primary btn-lg" %>
      </div>
    <% end %>
  </div>

  <!-- リンク -->
  <div class="text-center mt-4">
    <%= render "users/shared/links" %>
  </div>
</div>

