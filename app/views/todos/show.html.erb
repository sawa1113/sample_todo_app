<h1>Show Todo</h1>

<p>
  <strong>タイトル:</strong>
  <%= @todo.title %>
</p>

<p>
  <strong>説明:</strong>
  <%= @todo.description %>
</p>

<p>
  <strong>添付ファイル:</strong>
  <% if @todo.attachment.present? %>
    <%= link_to @todo.attachment.url, target: "_blank" do %>
      <%= image_tag @todo.attachment.url(:thumb), alt: "添付ファイルのサムネイル" %>
    <% end %>
  <% end %>
</p>

<p>
  <strong>タスクの状態:</strong>
  <%= @todo.completed? ? "完了" : "未完了" %>
</p>


<%= link_to '編集', edit_todo_path(@todo) %>
<%= link_to '一覧へ戻る', todos_path %>